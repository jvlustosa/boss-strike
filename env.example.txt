# Supabase Configuration
# Copy this file to .env and fill in your Supabase credentials
# Get these values from your Supabase project settings:
# Project Settings → API → Project URL and anon public key

# ⚠️ SECURITY WARNING ⚠️
# NEVER use SUPABASE_SECRET_KEY in the frontend!
# The secret key has admin privileges and should ONLY be used in server-side code.
# If you use VITE_ prefix, the variable will be exposed in the client bundle.

# Frontend variables (safe to expose)
VITE_SUPABASE_URL=your_supabase_project_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Backend/Server variables (DO NOT use VITE_ prefix for these!)
# Only use these in server-side code (Node.js, API routes, etc.)
# SUPABASE_SECRET_KEY=your_secret_key_here  # ⚠️ NEVER in frontend!

# Example:
# VITE_SUPABASE_URL=https://abcdefghijklmnop.supabase.co
# VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImFiY2RlZmdoaWprbG1ub3AiLCJyb2xlIjoiYW5vbiIsImlhdCI6MTY0NTIzNDU2NywiZXhwIjoxOTYwODEwNTY3fQ.example_key_here

